name: OpenWrt

on:
  workflow_dispatch:
  repository_dispatch:
    types:
      - OpenWrt

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: "ubuntu-24.04"

    env:
      DEBIAN_FRONTEND: noninteractive
      build_os: "cachyos"
      build_success: ""

    steps:
    - if: github.event.repository.owner.id == github.event.sender.id
      name: 显示系统信息
      run: |
        echo -e "💬 Server information \n"
        echo -e "💻 Server running on Ubuntu: [ Release: $(cat /etc/os-release | grep VERSION_CODENAME | cut -d '=' -f2) / Host: $(arch) ] \n"
        echo -e "🧮 Server CPU configuration information: \n$(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
        echo -e "💾 Server memory usage: \n$(free -h) \n"
        echo -e "🗃️ Server space usag: \n$(df -hT ${{ github.workspace }}) \n"

    - name: 准备可用空间
      uses: AdityaGarg8/remove-unwanted-software@v5
      with:
        remove-dotnet: true
        remove-android: true
        remove-haskell: true
        remove-codeql: true
        remove-docker-images: true
        remove-large-packages: true
        remove-cached-tools: true
        remove-swapfile: true

    - name: 检出代码
      uses: actions/checkout@v6

    - name: 准备容器 ( init.sh )
      run: bash scripts/init.sh

    - name: 准备源码 ( prepare.sh )
      run: d "cd ${ffdir} && bash scripts/openwrt/prepare.sh"

    - name: 准备编译
      run: |
        d '
          p "处理菜单"
          cd ${wrtdir}/feeds/luci && bash ${ffdir}/scripts/menu.sh
          cd ${wrtdir}/package/add && bash ${ffdir}/scripts/menu.sh

          p "展开配置"
          cd ${wrtdir}
          cp ${ffdir}/scripts/openwrt/config.seed .config
          make defconfig
        '

    - name: 压缩编译目录
      run: |
        d '
          cd ${workdir}
          cp -r ${ffdir} ${wrtdir}/
          tar -cf - $(basename ${wrtdir})/ | zstd -19 -T0 > wrt_end.tar.zst
          rm -rf ${wrtdir}/$(basename ${ffdir})
        '

    - name: 上传编译目录
      uses: actions/upload-artifact@main
      with:
        name: wrt_end
        path: ${{ env.workdir_host }}/wrt_end.tar.zst
        compression-level: 0

    - name: 缓存
      uses: HiGarfield/cachewrtbuild@main
      with:
        mixkey: openwrt
        prefix: ${{ env.workdir_host }}/openwrt

    - name: 开始编译 OpenWrt
      run: |
        d '
          cd ${wrtdir}
          p "开始下载"
          make download -j$(($(nproc) + 1))
          rm -rf feeds/luci/applications/luci-app-samba4 feeds/packages/net/samba4
          p "开始编译"
          make -j$(($(nproc) + 1))
          if [ $? -ne 0 ]; then
            p "！！编译失败！！开启单线程"
            make -j1 V=s
          else
            p "编译成功"
            . set_env build_success "true"
          fi

          p "打包日志"
          tar -cf - logs/ | zstd -19 -T0 > "${workdir}/logs_openwrt.tar.zst"

          [ "${build_success}" == "true" ] || exit 1
        '

        p "打印剩余空间"
        df -h

    - name: 上传日志文件
      uses: actions/upload-artifact@main
      with:
        name: logs
        path: ${{ env.workdir_host }}/logs_openwrt.tar.zst
        compression-level: 0

    - name: 打包固件
      uses: ophub/amlogic-s9xxx-openwrt@main
      with:
        openwrt_path: ${{ env.workdir_host }}/openwrt/bin/targets/*/*/*rootfs.tar.gz
        openwrt_board: 905d
        openwrt_kernel: 6.1.y_6.6.y_6.12.y_6.18.y
        openwrt_ip: 192.168.1.99
        #build_date: ${{ env.build_date }}

    - name: Upload the packaged OpenWrt
      uses: ncipollo/release-action@main
        #if: ${{ env.PACKAGED_STATUS }} == 'success' && !cancelled()
        #if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
      with:
          tag: openwrt_amlogic_N1_${{ env.PACKAGED_OUTPUTDATE }}
          artifacts: ${{ env.workdir_host }}/artifact/*.zip
          allowUpdates: true
          removeArtifacts: false
          replacesArtifacts: true
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
           Date：${{ env.PACKAGED_OUTPUTDATE }}
           OpenWrt image for N1 
           IP: 192.168.1.99
           username: root
           password: password
 
